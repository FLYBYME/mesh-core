{
    "name": "@flybyme/mesh-core",
    "version": "0.2.0",
    "description": "The parts the platform is operated with: the shell, and the consoles for the services behind it.",
    "type": "module",
    "private": true,
    "//exports": "Source, not dist: mesh-core has no build step because a part's entry is a TypeScript path that the builder compiles. A consumer resolving to src/ gets the same file the builder does.",
    "exports": {
        ".": "./src/index.ts",
        "./ui": "./src/ui/index.ts",
        "./auth": "./src/auth/index.ts",
        "./package.json": "./package.json"
    },
    "scripts": {
        "generate": "cd ../mesh-serve && node bin/mesh-serve.mjs generate --api \"$API_ID\" --out ../mesh-core/src/identity/generated/api.ts",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "test:browser": "vitest run --config vitest.browser.config.ts --no-file-parallelism",
        "test:source": "vitest run --environment node test/ui.source.spec.ts",
        "test": "npm run typecheck && npm run test:source && npm run test:browser"
    },
    "devDependencies": {
        "@flybyme/mesh-serve": "file:../mesh-serve",
        "@flybyme/mesh-web": "github:FLYBYME/mesh-web#v0.17.1",
        "@vitest/browser": "^2.1.9",
        "playwright": "^1.62.1",
        "typescript": "^5.9.3",
        "vitest": "^2.1.9"
    }
}
