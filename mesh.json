{
    "kernel": "^0.16",
    "parts": [
        {
            "kind": "extension",
            "id": "auth",
            "version": "0.1.0",
            "entry": "src/auth/index.ts",
            "kernel": "^0.16",
            "description": "The credential seam: holds the session, attaches the ticket, handles revocation. A site that has no accounts does not load it.",
            "keywords": ["auth", "session", "identity"],
            "license": "UNLICENSED",
            "homepage": "https://github.com/FLYBYME/mesh-core",
            "//mesh": "The three calls the Extension makes by path, declared so a site actually grants them. Without this a site could load auth and expose no way to sign in -- the api would 404 /api/identity/ticket and the page would report a network failure for a route that exists and was simply not granted.",
            "mesh": [
                {
                    "package": "@flybyme/mesh-serve",
                    "version": "0.1.0",
                    "contracts": [
                        "identity.ticket_issue",
                        "identity.ticket_revoke",
                        "identity.whoami",
                        "identity.set_password"
                    ]
                }
            ]
        },
        {
            "kind": "application",
            "id": "identity",
            "version": "0.1.0",
            "entry": "src/identity/index.ts",
            "kernel": "^0.16",
            "requiredParts": [
                { "id": "auth", "version": "^0.1" }
            ],
            "description": "Who may act, and where. Organizations, their members, and the roles those members hold.",
            "keywords": ["identity", "organizations", "access"],
            "license": "UNLICENSED",
            "homepage": "https://github.com/FLYBYME/mesh-core",
            "mesh": [
                {
                    "package": "@flybyme/mesh-serve",
                    "version": "0.1.0",
                    "//contracts": "The key is `contracts`, not `consumes`. `requirementsOf` flat-maps `mesh[].contracts` and reads nothing else, so this list declared no requirement at all and a site built from it would grant identity nothing -- the same empty-descriptor failure flowboard had.",
                    "contracts": [
                        "organization.find",
                        "organization.create",
                        "membership.find",
                        "membership.create",
                        "membership.delete",
                        "role.find",
                        "identity.whoami"
                    ]
                }
            ]
        }
    ]
}
